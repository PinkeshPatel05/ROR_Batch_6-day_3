<%# <p style="color: green"><%= notice %>

<%= render @task %>

<div>
  <% if can? :update, @task %><%= link_to "Edit this task", edit_task_path(@task), class:"btn btn-secondary" %> <%end%>
  <%= link_to "Back to tasks", tasks_path, class:"btn btn-secondary" %>
<br/>
  <br/><% if can? :destroy, @task %><%= button_to "Destroy this task", @task, method: :delete%><%end%>
</div>
